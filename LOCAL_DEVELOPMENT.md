# 🚀 Vue 聊天室本地开发指南

本项目专注于本地开发，提供完整的聊天室功能。

## 项目结构

本项目包含两个主要部分：
1. **Vue.js 前端应用** - 用户界面和交互
2. **Node.js 聊天服务器** - WebSocket 通信后端

## 本地开发步骤

### 步骤 1: 安装依赖
```bash
npm install
```

### 步骤 2: 启动聊天服务器
```bash
npm run chat-server
```
服务器将在 `http://localhost:3001` 启动

### 步骤 3: 启动前端开发服务器
```bash
npm run dev
```
前端将在 `http://localhost:5173` 启动

### 步骤 4: 测试聊天功能
1. 打开浏览器访问 `http://localhost:5173`
2. 导航到聊天页面
3. 点击"真实连接"按钮
4. 输入用户名开始聊天

## 功能特性

- 实时聊天消息
- 在线用户列表
- 用户加入/离开通知
- 输入状态显示
- 演示模式（无需服务器）
- 响应式设计

## 开发脚本

- `npm run dev` - 启动前端开发服务器
- `npm run build` - 构建生产版本
- `npm run chat-server` - 启动聊天服务器
- `npm run chat-server:dev` - 启动聊天服务器（开发模式，自动重启）

## 故障排除

### 聊天连接问题
1. 确保聊天服务器已启动
2. 检查端口 3001 是否可用
3. 查看浏览器控制台错误信息

### 常见错误
- **EADDRINUSE**: 端口被占用，请关闭其他使用 3001 端口的程序
- **连接超时**: 检查防火墙设置
- **CORS 错误**: 重启两个服务器

## 技术栈

- **前端**: Vue 3, Element Plus, Socket.IO Client
- **后端**: Node.js, Express, Socket.IO
- **开发工具**: Vite, nodemon

## 开发提示

1. 聊天服务器需要先启动，否则前端连接会失败
2. 可以打开多个浏览器标签页测试多用户聊天
3. 演示模式可以在不启动服务器的情况下体验功能
4. 所有配置都已针对本地开发优化
